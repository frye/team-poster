const Engineer = require('../lib/Engineer');

describe('Engineer', () => {
	describe('Initialization', () => {
		it.todo('should create an object with github username if provided with valid arguments');
	});
});