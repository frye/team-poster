const Employee = require('../lib/Employee');

describe('Employee', () => {
	describe('Initialization', () => {
		it.todo('should create an object with name, id and email if provided with valid arguments');
	});
});