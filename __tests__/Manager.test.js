const Manager = require('../lib/Manager');

describe('Manager', () => {
	describe('Initialization', () => {
		it.todo('should create an object with officeNumber if provided with valid arguments');
	});
});